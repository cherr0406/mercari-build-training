CREATE TABLE IF NOT EXISTS "items"(id integer PRIMARY KEY, name text, category_id integer, image_name string, FOREIGN KEY(category_id)  REFERENCES categories(id));
CREATE TABLE IF NOT EXISTS "categories"(id integer PRIMARY KEY, name text UNIQUE);
